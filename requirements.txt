openai
python-dotenv
pillow
# OpenCV 5.x dropped bundled Haar XMLs; keep 4.x for face-detect crop.
opencv-python>=4.8,<5
# Pin to daemon major.minor (check: curl localhost:8000/api/daemon/status).
# SDK 1.9 + daemon 1.8 hangs in GStreamer media init.
reachy-mini>=1.8.0,<1.9
# reachy-mini 1.8 pins hub==1.3.0; transformers/mlx-vlm need >=1.5 — keep hub newer.
huggingface-hub>=1.5.0,<2.0

# Only needed for the local (edge) models:
torch
transformers
torchvision
num2words
# Apple Silicon 4-bit path (bitsandbytes is CUDA-only):
mlx-vlm
# Optional — nudges/summaries no longer load an LLM in the live loop:
mlx-lm
